# Changelog

## 2026-05-29

- Moved the view-mode toggle from the left panel into the viewer canvas (top-right overlay) for faster in-scene switching.
- Repositioned the view-mode overlay deeper inside the sky screen so it sits within the viewing frame instead of hugging the page edge.
- Reduced left panel length by moving long mood/atmosphere controls into a bottom dock inside the viewer.
- Merged `Tonight mood` and `Atmosphere / sky tuning` into a single expandable bottom dock layout.
- Tightened panel spacing and text rhythm for cleaner scanning in watch mode.

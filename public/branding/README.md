Place brand assets here:

- `zeravue-logo.png` (home hero logo)
- `zeravue-profile.png` (top-right observer avatar)

These files are optional.  
If they are missing, the UI falls back to text-only rendering.

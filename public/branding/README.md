Place brand assets here:

- `zeravue-logo.svg` (default home hero logo)
- `zeravue-mark.svg` (default top-right observer avatar)
- `zeravue-logo.png` (optional fallback)
- `zeravue-profile.png` (optional fallback)

These files are optional.  
If they are missing, the UI falls back to text-only rendering.

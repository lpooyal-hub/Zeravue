Place loop-ready theme audio files in this folder and point `VITE_AMBIENT_TRACK_URL`
at the file you want to use for ambient playback.

The project currently includes a generated default loop:

`/audio/night-sky-loop.wav`

Example:

`VITE_AMBIENT_TRACK_URL=/audio/night-sky-loop.mp3`
